{
  "name": "kta-workforce",
  "version": "3.7.6",
  "private": true,
  "type": "module",
  "scripts": {
    "dev":        "vite",
    "build":      "vite build",
    "preview":    "vite preview",
    "deploy:beta": "vercel --force 2>&1 | tee /tmp/vercel-out.txt && vercel alias set $(grep -o 'https://kta-[^ ]*\\.vercel\\.app' /tmp/vercel-out.txt | head -1) kta-app-beta.vercel.app"
  },
  "dependencies": {
    "react":     "^18.2.0",
    "react-dom": "^18.2.0",
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^5.0.0"
  }
}
